PORT=4173

# Optional production virtual try-on provider.
# The app sends a JSON payload to this URL and expects either:
# { "imageUrl": "https://..." } or { "imageBase64": "..." }
TRY_ON_API_URL=
TRY_ON_API_KEY=
